name: techm_practice
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # 🔥 Firebase (COMPATIBLE SET)
  firebase_core: ^3.15.2
  firebase_auth: ^5.7.0
  cloud_firestore: ^5.4.4
  firebase_storage: ^12.1.3

  # 🖼 Media
  image_picker: ^1.1.2
  video_player: ^2.8.2

  # 😊 Emoji picker (UPDATED – FIXES CONFLICT)
  emoji_picker_flutter: ^3.0.0

  # 🔄 State management
  provider: ^6.0.5

  # 🔐 Google Auth
  google_sign_in: ^6.2.1

  # 🌐 Explicit web dependency (VERY IMPORTANT)
  web: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/images/
